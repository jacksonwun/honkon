export interface ThumbBox {
  cat: string;
  title: string;
  picURL: string;
  slug: string;
}

export interface ThumbBoxWithDes {
  cat: string;
  title: string;
  picURL: string;
  slug: string;
  des: string;
}
